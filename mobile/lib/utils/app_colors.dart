import 'package:flutter/material.dart';

// Paleta de cores baseada no "Layout geral — AntiBet"
class AppColors {
  // Fundo principal (Neutralidade, leveza)
  static const Color mainBackground = Color(0xFFF7F8F9); 

  // Texto principal (Leitura confortável)
  static const Color textPrimary = Color(0xFF1A1A1A); 

  // Texto secundário (Apoio sem sobrecarregar)
  static const Color textSecondary = Color(0xFF6B6B6B); 

  // -----------------------------------------------------------------
  // CORES PRIMÁRIAS E DE AÇÃO
  // -----------------------------------------------------------------

  // Botões/realce (Confiança, foco no controle)
  static const Color primaryBlue = Color(0xFF283593); 

  // Progresso, segurança (Ações positivas)
  static const Color positiveGreen = Color(0xFF2E7D32); 
  
  // Alerta de risco moderado (Atenção, autoconsciência)
  static const Color warningOrange = Color(0xFFFF9800); 

  // Perigo, interrupção urgente (Alerta de risco alto)
  static const Color dangerRed = Color(0xFFD32F2F); 
}